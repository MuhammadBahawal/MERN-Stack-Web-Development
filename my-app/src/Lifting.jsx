import React from 'react'

const Lifting = ({onchange, prop}) => {
    use
    
  return (
    <div>
      <button onClick={()=>{onchange("hello")}}>click</button>
    </div>
  )
}

export default Lifting
